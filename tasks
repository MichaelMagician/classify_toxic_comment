
calibration

config
    enable calibration
    output

run:
    predictor.train_calibrator()
    predictor.predict()

preditor
    train_calibrator
        calibrator.fit()
    predict_raw    
    predict_prob
        predict_raw and calibrate prob
    calibrate
        calibrate prob of each feature    

calibrator
    pick model
    calculate_ece
    fit(prob, true_labels)
    calibrate(prob)
    draw reliability chart and output to folder

